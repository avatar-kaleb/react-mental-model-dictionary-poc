import React, { PureComponent } from 'react';
import './Home.css';

class Home extends PureComponent {
  render() {
    return (
      <div className="App">
        <div>ADD LOGIN PAGE if user isnt logged in</div>
        <div>Add Landing Page if user is logged in</div>
      </div>
    );
  }
}

export default Home;
